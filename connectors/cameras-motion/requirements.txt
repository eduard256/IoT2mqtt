# Camera Motion Detection Connector Requirements
# Minimal dependencies for FFmpeg-based motion detection

# MQTT client (inherited from shared/)
# paho-mqtt==1.6.1  # Provided by base image

# No additional dependencies required
# FFmpeg is installed via apt-get in Dockerfile
